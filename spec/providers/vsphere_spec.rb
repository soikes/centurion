require 'spec_helper'

RSpec.describe Centurion::Provider::VSphere do
  describe '#initialize' do

  end

  describe '#create' do
    it 'clones a new vm from a template' do
      # subject.stub_chain()
      # expect(vsphere).to receive(:clone_vm).with('template','template_path','test','path')
      # vsphere.create('test','path','template_path/template')
    end
  end
end
